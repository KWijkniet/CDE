export default class Shape {
    #vertices = null;
    #shapebuffer = null;
    #textBuffer = null;

    constructor(vertices = []) {
        this.#vertices = vertices;
    }

    #generate = () => {

    };
}